<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title>Forgot Protected Folder PIN</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <style>
      /* Reset */
      body,
      html {
        margin: 0;
        padding: 0;
      }

      body {
        font-family: "Segoe UI", Tahoma, Geneva, Verdana, sans-serif;
        background: linear-gradient(135deg, #00b09b, #96c93d);
        color: #333;
        padding: 20px;
      }

      /* Container Card */
      .container {
        max-width: 600px;
        margin: 40px auto;
        background: #ffffff;
        border-radius: 14px;
        overflow: hidden;
        box-shadow: 0 10px 25px rgba(0, 0, 0, 0.15);
        animation: fadeInUp 0.8s ease;
      }

      /* Header */
      .header {
        background: linear-gradient(135deg, #00b09b, #96c93d);
        color: #fff;
        padding: 35px 20px;
        text-align: center;
        animation: slideDown 1s ease;
      }
      .header h1 {
        margin: 0;
        font-size: 26px;
        letter-spacing: 1px;
      }

      /* Content */
      .content {
        padding: 35px 25px;
        text-align: center;
      }
      .content h2 {
        font-size: 22px;
        margin-bottom: 20px;
        color: #00b09b;
        animation: fadeIn 1s ease 0.3s;
      }
      .content p {
        line-height: 1.6;
        margin: 12px 0;
        font-size: 15px;
        color: #444;
        animation: fadeIn 1s ease 0.6s;
      }

      /* Button */
      .btn {
        display: inline-block;
        margin: 30px 0;
        padding: 14px 32px;
        background: linear-gradient(135deg, #00b09b, #96c93d);
        color: #fff !important;
        text-decoration: none;
        border-radius: 50px;
        font-size: 16px;
        font-weight: 600;
        letter-spacing: 0.5px;
        box-shadow: 0 6px 15px rgba(0, 176, 155, 0.4);
        transition: transform 0.3s ease, box-shadow 0.3s ease;
        animation: pulse 1.5s infinite;
      }
      .btn:hover {
        transform: scale(1.05);
        box-shadow: 0 8px 18px rgba(0, 176, 155, 0.55);
      }

      /* Footer */
      .footer {
        background: #f7f7f7;
        text-align: center;
        padding: 18px;
        font-size: 12px;
        color: #666;
      }

      .otp-box {
        display: inline-block;
        background: #f4f7fb;
        border: 2px dashed #00b09b;
        border-radius: 8px;
        padding: 18px 28px;
        font-size: 26px;
        font-weight: bold;
        color: #333;
        letter-spacing: 4px;
        margin: 20px 0;
        animation: pulse 1.5s infinite;
      }

      /* Animations */
      @keyframes fadeInUp {
        from {
          opacity: 0;
          transform: translateY(30px);
        }
        to {
          opacity: 1;
          transform: translateY(0);
        }
      }
      @keyframes slideDown {
        from {
          opacity: 0;
          transform: translateY(-20px);
        }
        to {
          opacity: 1;
          transform: translateY(0);
        }
      }
      @keyframes fadeIn {
        from {
          opacity: 0;
        }
        to {
          opacity: 1;
        }
      }
      @keyframes pulse {
        0% {
          transform: scale(1);
        }
        50% {
          transform: scale(1.05);
        }
        100% {
          transform: scale(1);
        }
      }

      /* Responsive */
      @media (max-width: 600px) {
        .header h1 {
          font-size: 22px;
        }
        .content h2 {
          font-size: 18px;
        }
        .btn {
          width: 100%;
          text-align: center;
          padding: 14px 0;
        }
      }
    </style>
  </head>
  <body>
    <div class="container">
      <!-- Header -->
      <div class="header">
        <h1>🔒 Spark Drive</h1>
      </div>

      <!-- Content -->
      <div class="content">
        <h2>Protected Folder PIN Forgot Request</h2>
        <p>Hello <%= name %>,</p>
        <p>
          We received a request to reset your protected folder PIN. Please click
          the OTP below to confirm you are the validate user.
        </p>
        <p>Use the OTP code below to reset your protected folder PIN:</p>

        <div class="otp-box"><%= otp %></div>
        <p>
          ⚠️ If you did not request this OTP, please ignore this email or
          contact support immediately.
          <br />
          The forgot protected folder PIN otp will expire in
          <strong>2 minutes</strong>.
        </p>
      </div>

      <!-- Footer -->
      <div class="footer">
        &copy; <%= new Date().getFullYear() %> Spark Drive. All rights reserved.
      </div>
    </div>
  </body>
</html>
